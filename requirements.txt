# Database connectivity
pyodbc>=4.0.39

# Environment / secrets
python-dotenv>=1.0.0

# LLM backend — provider-agnostic via LiteLLM (OpenAI, Anthropic, Gemini,
# Azure, Bedrock, etc.); switch providers by changing LLM_MODEL in .env,
# no code or dependency change needed.
litellm>=1.40.0
