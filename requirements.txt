# Database connectivity
pyodbc>=4.0.39

# Environment / secrets
python-dotenv>=1.0.0

# LLM backend — OPTIONAL. Only needed for the in-process `--backend litellm`
# decision step (per-token API-key calls, provider-agnostic via LiteLLM:
# OpenAI, Anthropic, Gemini, Azure, Bedrock, ...; switch providers by changing
# LLM_MODEL in .env). The agent-driven `python -m scripts.session` path — where
# a coding agent (Claude Code, Codex) makes the decision itself — does NOT
# import litellm and needs no API key, so you can skip this line for that flow.
litellm>=1.40.0
